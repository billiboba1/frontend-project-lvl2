name: github-action

on: push

jobs: 
  build:
    runs-on: ubuntu-latest

    steps:
      -uses: action/checkout@v2

      -name: setup
       uses: action/setup-node@v1

      -name: install
       run: npm install

      -name: linter
       run: make lint

      -name: test
       run: make test

